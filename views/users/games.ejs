<% include ../partials/header-top %>

<title>yo! favo | <%= currentUser.first_name %>'s profile page</title>

<% include ../partials/header-bottom %>

<% include ../partials/profile-top %>

<!-- body content goes below here -->

games.ejs

<div class="row">
  <div class="col s12 l5 offset-l1 test-img-col">
    <p>img goes here</p>
  </div>
  <div class="col s12 l6">
    <div class="row">
      <div class="col s8 offset-s2">
        <h4>yo! top games</h4>
      </div>
    </div>
    <div class="row">
      <div class="col s8 offset-s2">
        <ul id="sortable">
          <% favos.forEach(function(favo) { %>
            <% if (favo.type === 'game') { %>
            <li id="<%= favo.id %>" class="ui-state-default"><span class="ui-icon ui-icon-arrowthick-2-n-s"></span><%= favo.name %></li>
            <% }; %>
          <% }); %>
        </ul>
      </div>
    </div>
    <div class="row">
      <div class="col s8 offset-s2">
        <a href="#">yo! top list</a>
      </div>
    </div>
  </div>
</div>


<!-- body content goes above here  -->

<% include ../partials/footer-content %>

<% include ../partials/footer %>
